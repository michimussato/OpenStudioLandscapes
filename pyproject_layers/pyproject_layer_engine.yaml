project:
  dependencies:
  - pydot
  - dagster-webserver==1.9.11
  - dagster-postgres==0.25.11
  - nox
  - human-readable-id
  description: A Toolkit to create dynamic Animation and VFX studio environments for
    robust automation and pipelines.
  name: OpenStudioLandscapes
  optional-dependencies:
#    pyproject:
#    - OpenStudioLandscapesUtil-VersionBumper @ git+https://github.com/michimussato/OpenStudioLandscapesUtil-VersionBumper.git
    dev:
    # Todo:
    #  - [ ] Can be implemented now
    #        - OpenStudioLandscapes[testing]
    - OpenStudioLandscapes[lint]
    # - OpenStudioLandscapes[nox]
    - OpenStudioLandscapes[sbom]
    # - OpenStudioLandscapes[coverage]
    - OpenStudioLandscapes[readme]
    - OpenStudioLandscapes[pyproject]
    - OpenStudioLandscapesUtil-VersionBumper @ git+https://github.com/michimussato/OpenStudioLandscapesUtil-VersionBumper.git
    # nox:
    # - OpenStudioLandscapes[testing]
    sbom:
    - OpenStudioLandscapes[graphviz]
  urls:
    Homepage: https://github.com/michimussato/OpenStudioLandscapes
  scripts:
    # Run Dagster default
    openstudiolandscapes: "OpenStudioLandscapes.cli:run"
    # Run Dagster job for example?
    # Thinking about an entry point for
    # cli interaction with Dagster
    # openstudiolandscapes-cli: "OpenStudioLandscapes.cli:run_cli"
tool:
  setuptools:
    include-package-data: true
    #package-dir = {"" = "src"}
    # Require a min/specific Python version (comma-separated conditions)
    # Add here dependencies of your project (line-separated), e.g. requests>=2.2,<3.0.
    # Version specifiers like >=2.2,<3.0 avoid problems due to API changes in
    # new major versions. This works if the required packages follow Semantic Versioning.
    # For more information, check out https://semver.org/.
    packages:
      find:
        exclude:
        # [options.package_data]
        # # * = *.txt, *.rst
        # hello = *.msg
        - tests
        namespaces: true
        where:
        - src
  # dagster:
  #   # We can define ONE SINGLE TOP-LEVEL code location
  #   # here in this pyproject.toml.
  #   # If you want to be able to define multiple
  #   # top-level code locations, comment out this
  #   # section and define them in a workspace.yaml
  #   # More info here:
  #   # https://docs.dagster.io/guides/build/projects/dagster-project-file-reference
  #   #
  #   # This should work, however:
  #   #
  #   # [tool.dagster]
  #   # modules = [
  #   #     { type = "module", name = "OpenStudioLandscapes.engine.definitions" },
  #   #     { type = "module", name = "Another.module.definitions" }
  #   # ]
  #   #
  #   # Dagster does not seem to support gRPC definitions here
  #   # (maybe to prevent storing of credentials). So,
  #   # back to workspace.yaml?
  #   module_name: OpenStudioLandscapes.engine.definitions
  #   project_name: OpenStudioLandscapes
