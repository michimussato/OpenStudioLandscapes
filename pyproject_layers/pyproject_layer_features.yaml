project:
  optional-dependencies:
    dev:
    - dagster-webserver==1.9.11
tool:
  setuptools:
    include-package-data: true
    package-dir:
      # This translates to:
      # package-dir = {"" = "src"}
      ? ''
      : src
